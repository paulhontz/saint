<%= Saint::FileServer.assets.js('ui.js') if http.xhr? %>

<%= saint_view.render_view('list/assoc/filters') if @relation.remote_node && http.params['xhr'].nil? && @attached == 0 %>

<% if (items = @remote_items) && items.size > 0 -%>

    <%= @pager.html %>
    <%= saint_view.render_partial('list/assoc/has_n-partial', items: items, level: 0, color: nil) %>

<% end -%>
